sudo: required
language: python
python: '3.6'
services:
  - docker

before_script: cd integration

script:
  # Note: for local installs outside a virtual env, use pip3
  - pip install pipenv
  - pipenv sync
  - docker-compose pull
  - /bin/bash ./run.sh
  - cat logs/*
