sudo: required
language: python
python: '3.6'
services:
  - docker

install: pip install tox

before_script: cd integration

script:
  - docker-compose pull
  - docker build -t brewblox/integration .
  - /bin/bash ./run.sh
  - cat logs/*
